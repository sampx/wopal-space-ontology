# DESIGN — <Topic>

> **Status**: Active
> **Updated**: YYYY-MM-DD
> **Parent**: `./DESIGN.md` — the main DESIGN this sub-document decomposes
> **Parent Architecture**: `<product-design-path or N/A>` — the product-level architecture this project follows
> **Parent Product**: `<product-prd-path or N/A>` — the product PRD this project follows

---

## <Topic Scope>

What this sub-document owns, in one or two sentences. State the boundary against the main DESIGN: the main document summarizes this topic and links here; the detail lives here.

---

## <First Topic Section>

Structure the body around the topic's own architecture: models, flows, contracts, data shapes, decision tables. Use `##` per major concept and `###` per sub-concept.

Keep the writing target-state: describe what exists and who owns it. No history, no migration notes, no implementation progress.

---

## Reference Documents

| Document | Purpose |
|----------|---------|
| `<path>` | <reference-only material; never a document already named in the header> |
