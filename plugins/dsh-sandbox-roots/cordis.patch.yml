# dsh-sandbox-roots: the stock sandbox rows are disabled and this bundle's
# replacements are inserted under their own ids — dsh skips an override row
# whose `name` differs from the standing row, so the swap is disable + insert.
# The service registrations (ctx.sandboxPolicy / ctx.sandbox / ctx.fs) come
# from the class chains, so consumers resolve the replacements by service name.
# Config values live in the `sandbox-policy` settings namespace (settings.yaml).

- id: sandbox-policy
  disabled: true
- id: sandbox
  disabled: true
- id: fs-sandbox
  disabled: true

- insert:
    - id: sandbox-roots-policy
      name: dsh-sandbox-roots
      config:
        mode: !!js process.env.DSH_PERMISSION_MODE ?? 'workspace-write'
        workspaceRoot: !!js process.cwd()
    - id: sandbox-roots-sandbox
      name: dsh-sandbox-roots/sandbox
    - id: sandbox-roots-fs
      name: dsh-sandbox-roots/fs